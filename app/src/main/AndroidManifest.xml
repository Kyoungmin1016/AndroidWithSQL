<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.androidwithsql">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidWithSQL">
        <activity
            android:name=".OrderRecyclerViewAdapter"
            android:exported="true"

            android:label="주문 현황"
            tools:ignore="Instantiatable" />

        <activity
            android:name=".GoodsFragment"
            android:exported="true"
            android:label="상품 목록"/>
        <activity
            android:name=".SeatActivity"

            android:exported="true"
            android:label="좌석표"/>
        <activity
            android:name=".GoodsActivity"
            android:exported="true"
            android:label="상품 주문"/>
        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:label="마이페이지"/>
        <activity
            android:name=".SignupActivity"
            android:exported="true"
            android:label="회원가입"/>
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="로그인"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="PC방 관리">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".TimeActivity"
            android:exported="true"
            android:label="시간 충전"/>


    </application>

</manifest>